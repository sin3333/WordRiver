import type { StreamConfig } from "./types/streamItemTypes";

export const Default_stream_config: StreamConfig = {
    baseDurationMs: 4500,
    fromOffsetY: 40,
    toOffsetY: 60,
    lanePaddingLeft: 16,
    MaxWidthRatio: 0.8,
    laneGap: 80,
};